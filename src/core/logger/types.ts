export type LogLevel = "debug" | "info" | "warn" | "error" | "success";

export enum DebugMode {
  hourly = "hourly",
  daily = "daily",
  weekly = "weekly",
  monthly = "monthly",
  yearly = "yearly",
}
